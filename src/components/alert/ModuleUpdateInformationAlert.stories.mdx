import { Meta, Story, Preview, Props, Description } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import ModuleUpdateInformationAlert from './ModuleUpdateInformationAlert.vue';

<Meta
  title="Sub-Components/Alerts/Alert module is updated"
  component={ModuleUpdateInformationAlert} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { ModuleUpdateInformationAlert },
  methods: {
    event: action('hasError'),
  },
  template: `<ModuleUpdateInformationAlert v-bind="$props" class="m-4" />`,
})

# Alert: Module is updated

When a v4 onboarded user update the ps accounts module on v5, 
a information alert should inform the user to reonboard with 
the same email and update other modules.

<Story name='Module is updated' height='220px'>
  {Template.bind({})}
</Story>

---
# Integration

```html
<ModuleUpdateInformationAlert />
```

# Technical details

<Description of={ModuleUpdateInformationAlert} />
<Props of={ModuleUpdateInformationAlert} />

