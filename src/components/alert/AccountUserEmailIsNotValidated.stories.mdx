import { Meta, Story, Preview, Props, Description } from '@storybook/addon-docs';
import { withKnobs, boolean, object } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';

import AccountUserEmailNotValidated from './AccountUserEmailNotValidated';

<Meta title="Sub-Components/Alerts/Alert user email is not validated" component={AccountUserEmailNotValidated} decorators={[withKnobs]} />

# Alert block: User email is not validated

When a user is connected with an account not verified. While the email is not validated, 
the user should be warned with that message.

<Story name='Not validated' height='220px'>
  {{
    components: {AccountUserEmailNotValidated},
    template: '<AccountUserEmailNotValidated class="m-4" resendEmailLink="/resend-email-validation-link" />',
  }}
</Story>

---
# Integration

```html
<AccountUserEmailNotValidated resendEmailLink="/resend-email-validation-link" />
```

# Technical details

<Description of={AccountUserEmailNotValidated} />
<Props of={AccountUserEmailNotValidated} />

