import { Meta, Story, Preview, Props, Description } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import AccountUserEmailNotValidated from './AccountUserEmailNotValidated.vue';

<Meta
  title="Sub-Components/Alerts/Alert user email is not validated"
  argTypes={{
    ssoResendVerificationEmail: {
      control: {
        type: 'text',
      },
    },
  }}
  component={AccountUserEmailNotValidated} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AccountUserEmailNotValidated },
  methods: {
    event: action('hasError'),
  },
  template: `<AccountUserEmailNotValidated v-bind="$props" class="m-4" />`,
})

# Alert block: User email is not validated

When a user is connected with an account not verified. While the email is not validated, 
the user should be warned with that message.

<Story
  name='Not validated'
  height='220px'
  args={{
    ssoResendVerificationEmail: '/resend-email-validation-link',
  }}>
  {Template.bind({})}
</Story>

---
# Integration

```html
<AccountUserEmailNotValidated ssoResendVerificationEmail="/resend-email-validation-link" />
```

# Technical details

<Description of={AccountUserEmailNotValidated} />
<Props of={AccountUserEmailNotValidated} />

