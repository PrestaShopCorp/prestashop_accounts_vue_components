import { ArgsTable, Canvas, Description, Meta, Story, Preview, Props } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import ShopUrlShouldExistsAlert from './ShopUrlShouldExistsAlert.vue';

<Meta
  title="Components/Alert"
  component={ShopUrlShouldExistsAlert} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { ShopUrlShouldExistsAlert },
  template: `<ShopUrlShouldExistsAlert v-bind="$props" />`,
})

# Alert: Shop Url Should Exists Alert

<Description of={ShopUrlShouldExistsAlert} />

<Canvas>
  <Story
    name='Shop Url Should Exists Alert'
    args={{
      hasAllShopsWithoutUrl: true,
      shopNamesWithoutUrl: ['Shop 1', 'Shop 2'],
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Shop Url Should Exists Alert" />

## Integration

```html
<ShopUrlShouldExistsAlert
  :has-all-shops-without-url="true"
  :shop-names-without-url="['Shop 1', 'Shop 2']" />
```

<Props of={ShopUrlShouldExistsAlert} />

