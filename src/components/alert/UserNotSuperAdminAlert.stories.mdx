import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import UserNotSuperAdminAlert from './UserNotSuperAdminAlert.vue';

<Meta
  title="Components/Alert"
  component={UserNotSuperAdminAlert} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { UserNotSuperAdminAlert },
  template: `<UserNotSuperAdminAlert v-bind="$props" />`,
})

# Alert: User Not Superadmin Alert

<Description of={UserNotSuperAdminAlert} />

<Canvas>
  <Story
    name='User Not Superadmin Alert'
    args={{
      superAdminEmail: 'email@example.org',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="User Not Superadmin Alert" />

## Integration

```html
<UserNotSuperAdminAlert super-admin-email="email@example.org" />
```

<ArgsTable of={UserNotSuperAdminAlert} />
