import { Meta, Story, Preview, Props, Description } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import EventBusNotInstalled from "./EventBusNotInstalled.vue"

<Meta
  title="Sub-Components/Alerts/Alert event bus is not installed"
  argTypes={{
    link: {
      control: {
        type: 'text',
      },
    },
    psIs17: {
      control: {
        type: 'boolean',
      },
    },
  }}
  component={EventBusNotInstalled} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { EventBusNotInstalled },
  methods: {
    event: action('hasError'),
  },
  template: `<EventBusNotInstalled v-bind="$props" @hasError="event" class="m-4" />`,
})

# Alert block: PS Event Bus module is not installed

When the PS Event Bus module is not installed in the module manager of the
backoffice a message warns the user that the module needs to be installed.

<Story
  name='Not installed'
  height='220px'
  args={{
    link: 'https://link',
    psIs17: true,
  }}>
  {Template.bind({})}
</Story>

---
# Integration

_Notice: If you need to integrate the standard behavior of PS Accounts, please see the
[`PsAccounts` component in the integration section](?path=/docs/integration-psaccounts-panel--connected)._

If you need a custom integration of PS Accounts sub-components, here is the EventBusNotInstalled block:

```html
<EventBusNotInstalled :link="link" :ps-is17="psIs17" @hasError="event" />
```

# Technical details

<Description of={EventBusNotInstalled} />
<Props of={EventBusNotInstalled} />
