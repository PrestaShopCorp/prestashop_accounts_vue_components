import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import ModuleAlert from './ModuleAlert.vue';

<Meta
  title="Components/Alert/Module Alert"
  component={ModuleAlert} />

export const Template = (args, { argTypes }) => ({
  components: { ModuleAlert },
  props: Object.keys(argTypes),
  methods: {
    onAction: action('action'),
  },
  template: `<ModuleAlert v-bind="$props" @action="onAction" />`,
})

# Module Alert: An alert for module needed action

<Description of={ModuleAlert} />

<Canvas>
  <Story
    name='Module Alert'
    args={{
      title: 'Activate the PrestaShop Event Bus module',
      message: 'You need the PrestaShop Event Bus module to continue setting up this module.',
      actionText: 'Activate',
      loading: false,
      loadingText: 'The PrestaShop Event Bus module is currently being activated...',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Module Alert" />

## Integration

```html
<ModuleAlert
  title="Activate the PrestaShop Event Bus module"
  message="You need the PrestaShop Event Bus module to continue setting up this module."
  action-text="Activate"
  :loading="false"
  loading-text="The PrestaShop Event Bus module is currently being activated..."
  @action="onAction"
  />
```

<ArgsTable of={ModuleAlert} />

