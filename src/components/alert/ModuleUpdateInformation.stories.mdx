import { Meta, Story, Preview, Props, Description } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import ModuleUpdateInformation from './ModuleUpdateInformation.vue';

<Meta
  title="Sub-Components/Alerts/Alert module is updated"
  component={ModuleUpdateInformation} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { ModuleUpdateInformation },
  methods: {
    event: action('hasError'),
  },
  template: `<ModuleUpdateInformation v-bind="$props" class="m-4" />`,
})

# Alert block: Module is updated

When a v4 onboarded user update the ps accounts module on v5, 
a information alert should inform the user to reonboard with 
the same email and update other modules.

<Story name='Module is updated' height='220px'>
  {Template.bind({})}
</Story>

---
# Integration

```html
<ModuleUpdateInformation />
```

# Technical details

<Description of={ModuleUpdateInformation} />
<Props of={ModuleUpdateInformation} />

