import { Meta, Story, Preview, Props, Description } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import UserEmailNotValidatedAlert from './UserEmailNotValidatedAlert.vue';

<Meta
  title="Sub-Components/Alerts/Alert user email not validated"
  argTypes={{
    ssoResendVerificationEmail: {
      control: {
        type: 'text',
      },
    },
  }}
  component={UserEmailNotValidatedAlert} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { UserEmailNotValidatedAlert },
  methods: {
    event: action('hasError'),
  },
  template: `<UserEmailNotValidatedAlert v-bind="$props" class="m-4" />`,
})

# Alert: User email not validated

When a user is connected with an account not verified. While the email is not validated, 
the user should be warned with that message.

<Story
  name='Not validated'
  height='220px'
  args={{
    ssoResendVerificationEmail: '/resend-email-validation-link',
  }}>
  {Template.bind({})}
</Story>

---
# Integration

```html
<UserEmailNotValidatedAlert ssoResendVerificationEmail="/resend-email-validation-link" />
```

# Technical details

<Description of={UserEmailNotValidatedAlert} />
<Props of={UserEmailNotValidatedAlert} />

