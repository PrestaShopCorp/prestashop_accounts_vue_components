import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import UserEmailNotValidatedAlert from './UserEmailNotValidatedAlert.vue';

<Meta
  title="Components/Alert"
  component={UserEmailNotValidatedAlert} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { UserEmailNotValidatedAlert },
  template: `<UserEmailNotValidatedAlert v-bind="$props" />`,
})

# Alert: User Email Not Validated Alert

<Description of={UserEmailNotValidatedAlert} />

<Story
  name='User Email Not Validated Alert'
  args={{
    ssoResendVerificationEmail: '/resend-email-validation-link',
  }}>
  {Template.bind({})}
</Story>

<ArgsTable story="User Email Not Validated Alert" />

## Integration

```html
<UserEmailNotValidatedAlert sso-resend-verification-email="/resend-email-validation-link" />
```

<ArgsTable of={UserEmailNotValidatedAlert} />
