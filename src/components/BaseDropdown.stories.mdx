import { ArgsTable, Canvas, Description, Meta, Story, Preview, Props } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import BaseDropdown, { Variant } from './BaseDropdown.vue';
import BaseDropdownItemButton from './BaseDropdownItemButton.vue';

<Meta
  title="Components/Dropdown"
  component={BaseDropdown} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { BaseDropdown, BaseDropdownItemButton },
  methods: {
    onClick: action('clicked'),
  },
  template: `
    <BaseDropdown v-bind="$props" @click="onClick">
      <BaseDropdownItemButton @click="onClick">Dropdown Button Item</BaseDropdownItemButton>
      <BaseDropdownItemButton @click="onClick">Dropdown Button Item</BaseDropdownItemButton>
    </BaseDropdown>
  `,
})

# Base Dropdown

<Description of={BaseDropdown} />

<Canvas>
  <Story
    name='Base Dropdown'
    args={{
      disabled: false,
      text: 'Dropdown button',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Base Dropdown" />

## Integration

```html
<BaseDropdown
  :disabled="false"
  text="Dropdown button"
  @click="onClick">
  <BaseDropdownItemButton @click="onClick">Dropdown Button Item</BaseDropdownItemButton>
  <BaseDropdownItemButton @click="onClick">Dropdown Button Item</BaseDropdownItemButton>
</BaseDropdown>
```

<Props of={BaseDropdown} />
