import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import BaseOverlay from './BaseOverlay.vue';

<Meta
  title="Components/Overlay"
  component={BaseOverlay}
  argTypes={{
    default: {
      control: 'text',
      defaultValue: 'Overlay content'
    },
  }} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { BaseOverlay },
  template: `
    <BaseOverlay v-bind="$props">
      {{ $props.default }}
    </BaseOverlay>
  `,
})

# Base Overlay

<Description of={BaseOverlay} />

<Canvas>
  <Story
    name='Base Overlay'
    args={{
      show: false,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Base Overlay" />

## Integration

```html
<BaseOverlay :show="false">
  overlay content
</BaseOverlay>
```

<ArgsTable of={BaseOverlay} />
